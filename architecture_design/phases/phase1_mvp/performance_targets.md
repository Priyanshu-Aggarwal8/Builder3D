# Phase 1 Performance Targets
- **Latency:** Layout generation < 2 seconds (CPU-bound simple calculations).  
- **Throughput:** Handle 100 requests/day easily, scalable to 1000+.  
- **Scalability:** Use auto-scaling containers (Kubernetes pods) if needed.
