# Phase 0 Features
- Minimal web UI to input plot dimensions and desired rooms.  
- Backend service that returns a *static sample* layout (no AI logic yet).  
- Collect user feedback (e.g. interactive drawing board).  
- Simple user authentication (optional).
